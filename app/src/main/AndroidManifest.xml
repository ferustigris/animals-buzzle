<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ferus.tigris.buzzles"
    android:versionCode="10"
    android:installLocation="auto" 
    android:versionName="unused resources have been removed" >

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:allowBackup="true"
        android:theme="@style/AppTheme" android:logo="@drawable/ic_launcher" android:name="ferus.tigris.buzzles.AnimalsBuzzles">
        <activity android:name="ferus.tigris.buzzles.Splash"
            android:theme="@style/Theme.Transparent" >
            
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="ferus.tigris.buzzles.GameFieldActivity" android:label="@string/title_activity_freeman_field" android:icon="@drawable/ic_launcher" android:logo="@drawable/ic_launcher" android:description="@string/title_activity_freeman_field" android:screenOrientation="portrait">
        </activity>
    </application>

</manifest>